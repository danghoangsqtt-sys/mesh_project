# Task 7.1: Unit Testing & Logic

## Objective
Write comprehensive unit tests for the core data manipulation and algorithmic logic of the application: `FrameExtractor` (handling telemetry and ACK packets) and `CommandPacket` (serialization). Ensure that the binary payload parsing logic operates reliably and fails safely on malformed data.

## Paths
- `e:\data\MyProject\2026\mesh_lilygo_app_android\app\src\test\java\com\meshcommand\app\comm\FrameExtractorTest.kt`
- `e:\data\MyProject\2026\mesh_lilygo_app_android\app\src\test\java\com\meshcommand\app\comm\CommandPacketTest.kt`

## File-Level Plan
1. **`CommandPacketTest.kt`**:
   - Write tests for `toByteArray()` to ensure the layout matches the 32-byte specification.
   - Test padding and string truncation behaviors.
2. **`FrameExtractorTest.kt`**:
   - Write tests simulating incoming UART bytes for a 40-byte SoldierTelemetry packet.
   - Write tests simulating incoming UART bytes for an 8-byte AckPacket.
   - Ensure `onPacketReceived` and `onAckReceived` callbacks are fired accurately.
   - Test fragmented streams (byte by byte) and garbage data interleaving.

## Best Practices
- Use JUnit 4/5.
- Keep tests fast and isolated. No Android dependencies or Context mocks.
