# Task 8.1 — PMTiles Downloader Backend API

## Objective
Tích hợp API tải bản đồ PMTiles theo Bounding Box.

## Paths
- `e:/data/2.MyProject/2026/mesh_pi5_server/backend/app/routers/map.py`
- `e:/data/2.MyProject/2026/mesh_pi5_server/backend/app/main.py`
- `e:/data/2.MyProject/2026/mesh_pi5_server/backend/app/services/map_service.py`

## File-Level Plan
1. **`app/services/map_service.py`**: Viết class `MapService` có hàm `download_pmtiles(bbox)`. Sử dụng subprocess gọi `pmtiles extract` hoặc dùng `httpx` tải file.
2. **`app/routers/map.py`**: Tạo endpoint `POST /api/maps/download` gọi tới service.
3. **`app/main.py`**: Include router mới.

## Best Practices Checklist
- [ ] Chạy background task trong FastAPI (không làm block main thread).
- [ ] Bắn tiến độ tải xuống (0-100%) qua WebSocket `/ws`.
- [ ] Cài đặt gói `pmtiles` python hoặc nhúng binary.

## Verification
- Chạy cURL tới `/api/maps/download` và kiểm tra file `offline.pmtiles` được thay thế.
