# Task 8.2 — Map Manager Frontend UI

## Objective
Tạo giao diện quản lý bản đồ để gọi API download, hiển thị tiến độ và kích hoạt làm mới bản đồ khi tải xong.

## Paths
- `e:/data/2.MyProject/2026/mesh_pi5_server/extracted_mesh/frontend/src/components/MapManagerModal.tsx`
- `e:/data/2.MyProject/2026/mesh_pi5_server/extracted_mesh/frontend/src/pages/Dashboard.tsx` (or TacticalMap)
- `e:/data/2.MyProject/2026/mesh_pi5_server/extracted_mesh/frontend/src/services/api.ts`

## File-Level Plan
1. **`api.ts`**: Thêm hàm `downloadMap(minLon, minLat, maxLon, maxLat)`.
2. **`MapManagerModal.tsx`**: Modal cho phép nhập Bounding Box, nhấn "Tải Xuống", và lắng nghe sự kiện WebSocket `map_download_progress` để hiển thị thanh progress bar.
3. **`Dashboard.tsx`**: Thêm nút "Tải Bản Đồ" mở Modal. Khi nhận WebSocket `map_ready`, tự động trigger force render bản đồ.

## Best Practices Checklist
- [ ] Giao diện hiện đại (military style, dark theme).
- [ ] Xử lý state loading cẩn thận, block tương tác khi đang download.
- [ ] Lắng nghe WS message để cập nhật % real-time mượt mà.

## Verification
- Click nút tải -> Thấy progress bar chạy từ 0 đến 100 -> Modal tự đóng và TacticalMap tự load lại map mới.
